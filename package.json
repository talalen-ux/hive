{
  "name": "hive-dapp",
  "version": "0.1.0",
  "private": true,
  "description": "Hive Staking Vault — stake $HIVE, earn real fees",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "seed": "hardhat run scripts/seedLiquidity.ts",
    "frontend:dev": "cd frontend && next dev",
    "frontend:build": "cd frontend && next build"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/node": "^20.11.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "next": "^14.2.0",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2",
    "typescript": "^5.4.0"
  }
}
